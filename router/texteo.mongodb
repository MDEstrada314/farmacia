use ('farmaciaCampus')

db.Medicamentos.find().toArray();
let totales = [0,0,0]
data.map((e)=>{
    if(e.proveedor.nombre == 'ProveedorA'){
        totales[0]++;
    }
    if(e.proveedor.nombre == 'ProveedorB'){
        totales[1]++
    }
    else if(e.proveedor.nombre == 'ProveedorC'){totales[2]++}
})
const mediProvee = [
    {proveedorA: totales[0]},
    {proveedorB: totales[1]},
    {proveedorC: totales[2]},
]